# CNM Server Configuration
# Copy this file to .env and configure

# Client authentication token - use a secure random string
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
RELAY_AUTH_TOKEN=your-secure-token-here

# Agent authentication token - agents must use this to register
CNM_AGENT_TOKEN=your-agent-token-here

# Optional: GitHub webhook secret for auto-deploy
# GITHUB_WEBHOOK_SECRET=your-webhook-secret
